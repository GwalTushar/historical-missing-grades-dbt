name: shopstream_dbt_demo
version: '1.0.0'
config-version: 2

profile: shopstream_dbt_demo

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]

models:
  shopstream_dbt_demo:
    bronze:
      +enabled: false

    silver:
      +materialized: table
      +schema: dbo

    gold:
      +materialized: table
